services:
  coolify:
    build:
      context: https://github.com/makeouthillx32/coolify.git#v4.x
      dockerfile: ./docker/development/Dockerfile
      args:
        - USER_ID=${USERID:-1000}
        - GROUP_ID=${GROUPID:-1000}
    ports:
      - "${APP_PORT:-8002}:8080"
    environment:
      AUTORUN_ENABLED: false
      PHP_OPCACHE_ENABLE: 1
      PUSHER_HOST: "${PUSHER_HOST}"
      PUSHER_PORT: "${PUSHER_PORT}"
      PUSHER_SCHEME: "${PUSHER_SCHEME:-http}"
      PUSHER_APP_ID: "${PUSHER_APP_ID:-coolify}"
      PUSHER_APP_KEY: "${PUSHER_APP_KEY:-coolify}"
      PUSHER_APP_SECRET: "${PUSHER_APP_SECRET:-coolify}"
    volumes:
      - dev_coolify_data:/data/coolify
      - dev_backups_data:/var/www/html/storage/app/backups
      - /var/run/docker.sock:/var/run/docker.sock
    working_dir: /var/www/html
    networks:
      - coolify
    depends_on:
      - postgres
      - redis
    restart: unless-stopped
      
  postgres:
    image: postgres:15-alpine
    pull_policy: always
    ports:
      - "5433:5432"  # Your preferred external port
    env_file:
      - .env
    environment:
      POSTGRES_USER: "${DB_USERNAME:-coolify}"
      POSTGRES_PASSWORD: "${DB_PASSWORD:-password}"
      POSTGRES_DB: "${DB_DATABASE:-coolify}"
    volumes:
      - dev_postgres_data:/var/lib/postgresql/data
    networks:
      - coolify
    restart: unless-stopped
      
  redis:
    image: redis:7-alpine
    pull_policy: always
    ports:
      - "6380:6379"  # Your preferred external port
    env_file:
      - .env
    # ADD PASSWORD AUTHENTICATION
    command: redis-server --requirepass ${REDIS_PASSWORD:-}
    volumes:
      - dev_redis_data:/data
    networks:
      - coolify
    restart: unless-stopped
      
  soketi:
    build:
      context: https://github.com/makeouthillx32/coolify.git#v4.x
      dockerfile: ./docker/coolify-realtime/Dockerfile
    env_file:
      - .env
    ports:
      - "6002:6001"  # Maps to your preferred 6002 port
      - "6003:6002"  # Terminal server port
    environment:
      SOKETI_DEBUG: "false"
      SOKETI_DEFAULT_APP_ID: "${PUSHER_APP_ID:-coolify}"
      SOKETI_DEFAULT_APP_KEY: "${PUSHER_APP_KEY:-coolify}"
      SOKETI_DEFAULT_APP_SECRET: "${PUSHER_APP_SECRET:-coolify}"
    networks:
      - coolify
    restart: unless-stopped
      
  vite:
    image: node:20-alpine
    pull_policy: always
    working_dir: /var/www/html
    environment:
      VITE_HOST: "${VITE_HOST:-localhost}"
      VITE_PORT: "${VITE_PORT:-5173}"
    ports:
      - "${VITE_PORT:-5173}:${VITE_PORT:-5173}"
    volumes:
      - .:/var/www/html/:cached
    command: sh -c "npm install && npm run dev"
    networks:
      - coolify
      
  testing-host:
    build:
      context: https://github.com/makeouthillx32/coolify.git#v4.x
      dockerfile: ./docker/testing-host/Dockerfile
    init: true
    container_name: coolify-testing-host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - dev_coolify_data:/data/coolify
      - dev_backups_data:/data/coolify/backups
      - dev_postgres_data:/data/coolify/_volumes/database
      - dev_redis_data:/data/coolify/_volumes/redis
      - dev_minio_data:/data/coolify/_volumes/minio
    networks:
      - coolify
    restart: unless-stopped
      
  mailpit:
    image: axllent/mailpit:latest
    pull_policy: always
    container_name: coolify-mail
    ports:
      - "1026:1025"    # Your preferred external port
      - "8026:8025"    # Your preferred external port
    networks:
      - coolify
    restart: unless-stopped
      
  minio:
    image: minio/minio:latest
    pull_policy: always
    container_name: coolify-minio
    command: server /data --console-address ":9001"
    ports:
      - "9001:9000"    # Your preferred external port
      - "9002:9001"    # Your preferred external port
    environment:
      MINIO_ACCESS_KEY: "${MINIO_ACCESS_KEY:-minioadmin}"
      MINIO_SECRET_KEY: "${MINIO_SECRET_KEY:-minioadmin}"
    volumes:
      - dev_minio_data:/data
    networks:
      - coolify
    restart: unless-stopped

volumes:
  dev_backups_data:
  dev_postgres_data:
  dev_redis_data:
  dev_coolify_data:
  dev_minio_data:

networks:
  coolify:
    name: coolify
    external: false